# ── Core (required for Phases 1-3) ──────────────────────────────────────────
openai>=1.30.0          # OpenAI-compatible SDK (works with Ollama, Groq, Gemini)
psycopg2-binary>=2.9.9  # PostgreSQL driver (for export_corpus.py)

# ── Phase 4: LoRA fine-tuning ────────────────────────────────────────────────
torch>=2.2.0
transformers>=4.40.0
peft>=0.10.0
trl>=0.8.6
datasets>=2.19.0
accelerate>=0.29.0
bitsandbytes>=0.43.0    # 4-bit quantization (Linux/macOS only)
